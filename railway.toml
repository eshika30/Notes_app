[build]
builder = "nixpacks"

[build.env]
JAVA_VERSION = "17"

[phases.setup]
nixPkgs = ["openjdk17", "maven"]

[phases.build]
cmds = ["mvn clean package -DskipTests"]

[start]
cmd = "java -jar target/*.jar"
